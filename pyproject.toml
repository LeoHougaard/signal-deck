[project]
name = "signal-deck"
version = "0.1.0"
description = "Local-first Obsidian idea radar agent"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
signal-deck = "signal_deck.app:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
